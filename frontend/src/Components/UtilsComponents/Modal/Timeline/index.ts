import TimelineModal from './TimelineModal';
import TimelinePage from './TimelinePage';

namespace Timeline {
	export const Modal = TimelineModal;
	export const Page = TimelinePage;
}

export default Timeline;
