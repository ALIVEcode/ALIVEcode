import Modal from '../../UtilsComponents/Modal/Modal';
import { FeedbackReportModalProps } from './NavbarTypes';

const FeedbackReportModal = ({ open, setOpen }: FeedbackReportModalProps) => {
	<Modal title="" setOpen={setOpen} open={open}>
		<div></div>
	</Modal>;
};

export default FeedbackReportModal;
